name: Daily Project Reminder

on:
  schedule:
    # All times UTC. Hours below target Eastern Daylight Time (UTC-4).
    - cron: "0 10 * * *"   # 6:00 AM ET -> morning message
    - cron: "0 16 * * *"   # 12:00 PM ET -> afternoon message
    - cron: "0 20 * * *"   # 4:00 PM ET -> evening message
  workflow_dispatch:
    inputs:
      slot:
        description: "Which message to send (for manual testing)"
        required: false
        default: "morning"
        type: choice
        options:
          - morning
          - afternoon
          - evening

jobs:
  send-reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Determine which slot to send
        id: slot
        run: |
          if [ -n "${{ github.event.inputs.slot }}" ]; then
            echo "slot=${{ github.event.inputs.slot }}" >> "$GITHUB_OUTPUT"
          else
            case "${{ github.event.schedule }}" in
              "0 10 * * *") echo "slot=morning" >> "$GITHUB_OUTPUT" ;;
              "0 16 * * *") echo "slot=afternoon" >> "$GITHUB_OUTPUT" ;;
              "0 20 * * *") echo "slot=evening" >> "$GITHUB_OUTPUT" ;;
              *) echo "slot=morning" >> "$GITHUB_OUTPUT" ;;
            esac
          fi

      - name: Send reminder
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SLOT: ${{ steps.slot.outputs.slot }}
        run: python send_reminder.py
